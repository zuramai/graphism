{
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "tsc && cross-env NODE_ENV=production vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "graphism": "workspace:0.2.0"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
